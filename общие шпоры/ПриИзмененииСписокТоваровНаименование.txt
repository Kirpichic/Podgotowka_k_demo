&НаСервере
Функция ПолучитьАртикул(Наименование)
	Возврат Наименование.Артикул;
КонецФункции     

&НаСервере
Функция ПолучитьЕдиницуИзмерения(Наименование)
	Возврат Наименование.ЕдиницаИзмерения;
КонецФункции

&НаКлиенте
Процедура СписокТоваровНаименованиеПриИзменении(Элемент)
	ТекСтрокаТЧ = Элементы.СписокТоваров.ТекущиеДанные;
	ТекСтрокаТЧ.Артикул = ПолучитьАртикул(ТекСтрокаТЧ.Наименование);
	ТекСтрокаТЧ.ЕдиницаИзмерения = ПолучитьЕдиницуИзмерения(ТекСтрокаТЧ.Наименование);
КонецПроцедуры
